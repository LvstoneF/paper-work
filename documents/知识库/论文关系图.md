# 论文关系图

> 论文间的显式关系网络。每新增一篇精读，追加该论文的关系条目。
>
> 关系类型：
> - 🔗 **互补** — 解决同一问题的不同子问题（合在一起才是完整方案）
> - ⚔️ **竞争** — 同一问题的不同解法（方法不同，目标相同）
> - 🔧 **同方法论** — 共享核心技术栈（可以互相借鉴实现细节）
> - ➡️ **前后继** — 后者基于前者改进或受前者启发（技术演进脉络）

---

## 已确认关系

### 🔗 互补关系

| 论文 A | 论文 B | 说明 |
|:---|:---|:---|
| VFArche (函数定位) | VERCATION (版本定位) | A 回答「漏洞函数是哪个」，B 回答「漏洞版本是哪个区间」。两者都是 SCA 可达性分析的前置步骤，互补但都止步于调用图可达性判定之前。 |
| VFArche / VERCATION (SCA 可达性层消减误报) | 保守分析 (SAST 报告层 LLM 研判消减误报) | 同属「误报消减」大目标的不同层次：前者在 SCA 组件可达性层面减少不可达告警，后者在 SAST 单条告警层面用 LLM 保守研判删误报。组合即覆盖 SCA→SAST 两道误报来源。 |
| 保守分析 (SAST 告警 LLM 保守研判) | IRIS (LLM 推断规范 + 上下文过滤 CodeQL 误报) | 都是「静态分析工具产出候选 → LLM 二次研判过滤」的两段式架构。保守分析做 fail-safe 删误报（D3●），IRIS 做规范推断+上下文过滤（无零漏报保证）——二者在纵轴上互补。 |

### 🔗 互补关系（续·T2L-Agent）

| 论文 A | 论文 B | 说明 |
|:---|:---|:---|
| T2L-Agent (crash trace → 行级定位) | CVE-Genie (CVE → exploit 闭环) | T2L 从运行时崩溃日志出发逆向定位漏洞代码行，CVE-Genie 从 CVE 描述出发正向生成 exploit。两者都是 Agent 驱动的漏洞定位，但数据起点不同（crash symptoms vs CVE description），技术栈互补（ATA 执行证据收集 vs 知识库+PoC 验证）。合起来覆盖「从漏洞发现到可利用性验证」的完整闭环。 |

### ⚔️ 竞争关系

| 论文 A | 论文 B | 说明 |
|:---|:---|:---|
| V-SZZ (git blame 追溯) | VERCATION (切片+LLM+AST) | 都做 vic 定位。V-SZZ 基于编辑距离回溯删除行（F1 80.1%），VERCATION 用 LLM+AST 展开处理重构（F1 93.1%）。VERCATION 的 32.2% 重构提交发现说明 V-SZZ 的根本局限。 |
| V0Finder (函数哈希) | VERCATION (语句级 AST) | 都做版本识别。V0Finder 对整个函数做哈希指纹（粗粒度），VERCATION 做语句级 AST 比对（细粒度）。F1: 78.5% vs 93.1%。 |

### 🔧 同方法论关系

| 论文 A | 论文 B | 说明 |
|:---|:---|:---|
| VERCATION (P1: 切片+LLM) | LLM4SA (程序依赖遍历+LLM) | 都采用「静态分析先粗筛 + LLM 再精筛」架构。VERCATION 针对版本定位，LLM4SA 针对 SAST 警告审查。 |
| IRIS (CodeQL+LLM 规范推断+上下文过滤) | VERCATION (切片+LLM 精筛) | 同属「静态工具主干 + LLM 辅助子任务」架构。IRIS 用 LLM 补规范+剪误报，VERCATION 用 LLM 筛版本区间；区别在于 IRIS 插入两个 LLM 组件而 VERCATION 只用一个。 |
| VERCATION (P1: 切片+LLM) | SAVANT (语义预处理+LLM) | 都面向代码分析做「静态预处理→LLM 理解和筛选」。SAVANT 更进一步做了 Java SCA 可达性场景。 |
| 保守分析 (SAST 告警+CoT/SC 重评估) | VERCATION (切片+LLM 精筛) | 都是「程序分析工具先产出候选 → LLM 二次研判筛选」的两段式架构。保守分析筛 SAST 误报，VERCATION 筛漏洞版本区间；二者与 LLM4SA 同源。 |

### 🔗 互补关系（续·SAVANT）

| 论文 A | 论文 B | 说明 |
|:---|:---|:---|
| SAVANT (语义级可达性判定) | VFArche (函数定位) | SAVANT 做「漏洞在应用中是否可触发」，VFArche 做「漏洞函数是哪个」——上下游关系。SAVANT 不依赖 VFArche 输出，但完整管线需两者组合。 |

### ⚔️ 竞争关系

| 论文 A | 论文 B | 说明 |
|:---|:---|:---|
| SAVANT (LLM+语义, 无调用图) | VERCATION (静态切片+LLM, 保留调用图) | 都做 SCA 精细化，但方法论分歧：SAVANT 完全放弃调用图用 LLM 语义替代，VERCATION 保留静态分析做切片再用 LLM 精筛。SAVANT F1 78.5% vs VERCATION F1 93.1%——但目标不同（SAVANT 判可达性，VERCATION 判版本区间），不直接可比。 |

### ➡️ 前后继关系

| 前作 | 后继 | 继承关系 |
|:---|:---|:---|
| V-SZZ (ICSE'22) | VERCATION (2024) | VERCATION 在 Related Work 中将 V-SZZ 作为主要 baseline 并改进了其「仅追踪删除行」和「无法处理重构提交」两个核心缺陷。 |
| 保守分析 wagner:2025 (D3●) | ZeroFalse iranmanesh:2025 (D3◐) | ZeroFalse 继承了保守分析「fail-safe」的思想，将其应用于 LLM 提升 SAST 精度场景。 |
| 保守分析 wagner:2025 (D3●) | Sifting-the-Noise xiong:2026 (D3◐) | 保守分析提供零漏报约束的保守消减方案，Sifting 展示无约束智能体方案的漏报代价（FN 22.25%）。两篇互补：一篇设安全保障天花板（能消多少不失），一篇测无约束地板（激进消减会失多少）。 |
| MERLIN/Seldon (概率推断规范) | IRIS li:2024 (LLM 推断规范) | IRIS 用 LLM 替代了 MERLIN/Seldon 的概率建模+线性优化方法来做污点规范推断——只需 API 名称和签名就能推断，不需要分析第三方库源码。 |

### 🔧 同方法论关系（续·Boosting）

| 论文 A | 论文 B | 说明 |
|:---|:---|:---|
| IRIS (CodeQL+LLM 规范推断+上下文过滤) | Boosting (CAFD: SVF+LLM 副作用判别) | 都是「静态分析工具主判 + LLM 辅助补判特定子问题」的混合架构。IRIS 让 LLM 补污点规范+上下文剪误报，Boosting 让 LLM 补 CAF 副作用可忽略性判定——LLM 承担的都是「规则无法穷举的语义分类」问题。 |
| SemTaint (LLM 解析 CodeQL 不可解析调用边) | Boosting (LLM 判定 CAF 副作用可忽略性) | 同属「LLM 补静态分析的精度缺口」范式。SemTaint 补调用图断边，Boosting 补指针分析的分配函数建模缺口——目标不同（JS taint vs C 指针分析），但技术思想高度一致。 |

### 🔗 互补关系（续·Boosting）

| 论文 A | 论文 B | 说明 |
|:---|:---|:---|
| IRIS (CodeQL 污点规范推断) | Boosting (SVF 自定义分配函数建模) | 分别补 CodeQL 和 SVF 两个静态分析框架的不同精度缺口。IRIS 让 CodeQL 知道哪些 API 是 source/sink，Boosting 让 SVF 知道哪些函数是 malloc-like。两者合起来覆盖了「静态分析的主干精度短板」——规范缺失 + 函数建模缺失。 |

### ⚔️ 竞争关系（续·VulAgent）

| 论文 A | 论文 B | 说明 |
|:---|:---|:---|
| VulAgent (假设验证多智能体) | VulTrial (法庭辩论多智能体) | 都做 LLM 多智能体漏洞检测。VulTrial 用法庭辩论式多角色反思（detector→judge→review board）判漏洞，VulAgent 用假设构造+路径验证+上下文校验判漏洞。VulAgent ACC 58.4% vs VulTrial 53.4% (+5.0pp)，P-C 26.6% vs 18.6% (+43%)，FPR 36.8% vs 52.6% (-30%)——假设验证优于文本辩论。 |
| VulAgent (假设验证) | GPTLens / LLM-MultiRole (多角色讨论) | 同为多智能体检测方法的迭代关系。GPTLens（auditor+critic）和 LLM-MultiRole（developer+tester）依赖 LLM 文本交互反思过滤 FP，VulAgent 用结构化假设+外部静态上下文替代纯文本反思，FPR 降幅更大。 |

### 🔧 同方法论关系（续·VulAgent）

| 论文 A | 论文 B | 说明 |
|:---|:---|:---|
| VulAgent (假设验证: Joern 上下文+LLM 路径判) | IRIS (CodeQL 上下文+LLM 规范推断+过滤) | 都使用「静态工具提取程序上下文 → LLM 基于上下文做精准决策」的两段式架构。IRIS 用 CodeQL 提取污点流上下文辅助 LLM 推断规范，VulAgent 用 Joern 提取 CFG/DFG/调用图辅助 LLM 验证假设——工具不同（CodeQL vs Joern）、任务不同（规范推断 vs 假设验证），但思想一致。 |
| VulAgent (LLM-native 检测+假设验证降 FP) | 保守分析 (SAST 告警+CoT/SC 降 FP) | 都做「滤误报」但层不同。保守分析在 SAST 告警层做 fail-safe 过滤（静态工具产候选 → LLM 判），VulAgent 在 LLM-native 检测层做假设验证过滤（LLM 自身产候选 → LLM 再验证）。两者可串联：SAST 告警 → 保守分析过滤 → LLM-native 检测 → VulAgent 验证。 |

### ➡️ 前后继关系（续·VulAgent）

| 前作 | 后继 | 继承关系 |
|:---|:---|:---|
| VulTrial (2025: 法庭辩论多智能体) | VulAgent (2025: 假设验证多智能体) | VulAgent 将 VulTrial 的「文本辩论式」过滤改进为「结构化假设+静态上下文验证」——不是让 LLM 自己跟自己争论，而是用 Joern 提取的程序事实验证 LLM 的检测结论。这是多智能体检测从「文本反思」走向「证据驱动」的关键演进。 |

---

## 按脉络组织的关系链

### 脉络一：静态分析与 LLM 的关系从「替代」转向「互补」

```
早期 (2022-2023): 纯 LLM 检测器
  ├── fu:2023 (ChatGPT 直接判漏洞)
  ├── gao:2023 (LLM 漏洞检测能力评估)
  └── zhang:2023 (Prompt 增强检测)
        │
        ▼ 转向互补
中期 (2024-2025): 静态 + LLM 联合
  ├── VERCATION (切片 + LLM 精筛) ─── 🔧 同方法论 ─── LLM4SA (依赖遍历 + LLM)
  ├── IRIS (LLM 辅助静态分析推理)
  └── SAVANT (语义预处理 + LLM 上下文感知)
        │
        ▼ 实证否定纯 LLM
晚期 (2025-2026): 混合管线成为共识
  ├── SAST-Genius (Semgrep + LLM, FP ↓91%)
  ├── ProjectScale (实证: Java 召回仅 34%, FDR 85% → 纯 LLM 不可行)
  └── SemTaint (CodeQL + LLM 补全断边)
  └── VulAgent (Joern 上下文 + 多智能体假设验证)
```

### 脉络二：调用图/CPG 成为 LLM 与程序分析的桥梁

```
传统程序分析
  ├── CodeQL (静态调用图)
  └── Joern (CPG: AST+CFG+DFG)
        │
        ▼ LLM 融合
图结构注入 LLM\n  ├── GRACE (AST+CFG+PDG → prompt, F1 ↑28.65%)\n  ├── LLMxCPG (CPG 引导 LLM 上下文感知)\n  └── Vul-LMGNNs (LM 嵌入 + GNN 传播) ✅
        │
        ▼ 关键空白
断边修复（D1 核心）
  └── SemTaint (LLM 解析 CodeQL 不可解析调用边) ← **唯一 D1●**
        └── ⚠️ 面向 JavaScript taint，非 Java SCA
```

### 脉络三：Agent 范式的「执行验证」共识

```
早期 Agent
  ├── T2L-Agent (崩溃 trace → 代码行)
  └── CVE-Genie (CVE → exploit 闭环)
        │
        ▼ 执行验证成标配
2026 Agent (D4● 聚集)
  ├── VIPER-MCP (反馈驱动提示进化 + PoC)
  ├── FORGE (5 Agent 分级利用 L0-L3)
  ├── Verify Before Fix (strict invariant: 无确认不修复)
  └── LLMVD.js (扫描→PoC→执行验证)
        │
        ▼ 关键偏离
验证目标差异
  └── 所有 Agent 验证「可利用性」(exploitability)
        ≠ SCA 需要「可达性」(reachability)
```

---

## 待建立的关系（下一批精读后填充）

以下关系来自骨架 §10 的分析但尚未形成精读笔记，待验证后录入：

| 关系类型 | 论文 A | 论文 B | 待验证假设 |
|:---|:---|:---|:---|
| 🔧 | SemTaint (D1●) | Bridging (D2●) | 若将 SemTaint 的「LLM 解析断边」+ Bridging 的「<4B 本地模型」结合 → 可同时获 D1●+D2● |
| 🔧 | 保守分析 (D3●) | SecureFixAgent (D2●) | 保守策略 + 离线小模型 → 零漏报离线部署 |
| ➡️ | VERCATION | 正文论文 | VERCATION 的「静态+LLM 协同」架构可直接复用到 Java 动态分发可达性补判管线 |
| ➡️ | SemTaint | 正文论文 | SemTaint 的「LLM 解析不可解析调用边」范式是可迁移的核心技术路线 |
| ➡️ | SAVANT | 正文论文 | SAVANT 的「PoV Test 驱动+LLM 语义理解」范式可复用，但其「完全放弃调用图」路线不可取——正文论文需走「静态调用图主判 + LLM 补判断边」路线 |
| 🔧 | VulAgent (假设验证) | 保守分析 (fail-safe) | VulAgent 用 Joern 上下文验证假设降噪，保守分析用 fail-safe 兜底。若将 VulAgent 的假设验证机制加上保守分析的零漏报约束 → 可同时获高精度降误报+零漏报保证 |
| ➡️ | VulAgent | 正文论文 | VulAgent 的「假设路径构造+静态上下文验证」范式可复用，但其完全依赖云端大模型（GPT-4o/DeepSeek v3.1/Qwen3-235B）且仅 C/C++ 函数级——正文论文需将其扩展到 Java 动态分发场景+离线小模型 |
| ➡️ | T2L-Agent | 正文论文 | T2L-Agent 验证了「运行时证据+Agent 架构」对精准漏洞定位的可行性（D4●），但其「必须可 Docker 编译 + 有 crash 日志」的前提条件反衬出正文论文面对的「不可编译离线场景」的独特性。正文论文可借鉴 T2L「两阶段精化（chunk→line）」的思路，但数据源需从「crash 日志」切换为「静态调用图+LLM 补判断边」，方法论从「Agent 主动执行」转向「LLM 辅助判定」。 |

---

## 更新规则

每次完成一篇新论文的精读后：
1. 识别该论文与已有论文的所有关系（逐篇过 4 种类型）
2. 追加到上表
3. 如果该论文形成新的脉络节点，更新对应的脉络图
4. 如果该论文与「正文论文」有直接技术关联（方法论可复用），标注 ➡️ 正文论文

### 🔧 同方法论关系（续·Vul-LMGNNs）

| 论文 A | 论文 B | 说明 |
|:---|:---|:---|
| Vul-LMGNNs (CodeLM+GNN+KD) | IRIS (LLM+CodeQL) | 同在方向④，都用代码图结构 + 神经/LLM 联合分析。IRIS 用 LLM 推断污点规范+过滤误报，Vul-LMGNNs 用 CodeLM 嵌入初始化 GNN + 在线知识蒸馏分类漏洞——方法论交叉：图结构注入与联合训练。 |
| Vul-LMGNNs (GGNN+CPG) | Devign (GGNN+CPG) | Vul-LMGNNs 直接继承 Devign 的 GGNN+CPG 架构（用 Devign 的 GGNN 实现 + 卷积池化输出层）并在此基础上添加 CodeLM 节点初始化和在线知识蒸馏。 |

### ⚔️ 竞争关系（续·Vul-LMGNNs）

| 论文 A | 论文 B | 说明 |
|:---|:---|:---|
| Vul-LMGNNs (GNN+CodeLM 联合训练) | LLMxCPG (CPG 引导 LLM 提示) | 同用 CPG 做漏洞检测但方法对立：Vul-LMGNNs 训练 GNN 和 CodeLM 端到端联合学习，LLMxCPG 用 CPG 结构信息构造 prompt 引导 LLM 推理——"训练模型" vs "提示模型"路线差异。 |
| Vul-LMGNNs (GNN+CodeLM 联合训练) | GRACE (AST+CFG+PDG 注入 LLM prompt) | 都注入代码结构到漏洞检测模型，方法对立：Vul-LMGNNs 用 GNN 独立建模图结构+CodeLM 互补，GRACE 把图结构序列化为文本注入 LLM prompt——"双模型联合" vs "全注入 LLM" 路线。 |

### ➡️ 前后继关系（续·Vul-LMGNNs）

| 前作 | 后继 | 继承关系 |
|:---|:---|:---|
| Devign (2019, GGNN+CPG 漏洞检测开创) | CSGVD (2023) → Vul-LMGNNs (2025) | Vul-LMGNNs 直接继承 Devign 的 GGNN+CPG 基础架构并在其文 Related Work 中明确引用 Devign。Devign 首次将 CPG+GNN 用于漏洞检测，CSGVD 加入序列+图联合嵌入，Vul-LMGNNs 进一步加入 CodeLM 初始化和在线知识蒸馏。 |
